name: tcg_seller_app
description: "Administración de subastas TCG conectada con Facebook Pages."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  camera: ^0.11.0+2
  image: ^4.3.0
  gal: ^2.3.0
  permission_handler: ^11.3.1
  native_camera_view: ^0.0.17+1
  supabase_flutter: ^2.10.0
  flutter_riverpod: ^2.6.1
  flutter_secure_storage: ^9.2.4
  intl: ^0.20.2
  timezone: ^0.10.1
  uuid: ^4.5.1
  url_launcher: ^6.3.2
  image_picker: ^1.1.2
  cached_network_image: ^3.4.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:

  uses-material-design: true
